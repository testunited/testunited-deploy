# Default values for testunited-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

core:
  service:
    type: LoadBalancer

env:
  name: stg
  namespace: testunited-stg
  host: stg.testunited.minikube.local 