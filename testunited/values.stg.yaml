# Default values for testunited-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

webapi:
  service:
    type: LoadBalancer
webui:
  service:
    type: LoadBalancer
env:
  name: stg
  namespace: testunited-stg
  host: stg.testunited.org 